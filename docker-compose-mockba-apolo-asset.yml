services:
  micro-mockba-asset-futures-bot:
    image: andresdom2004/micro-mockba-asset-futures-bot:latest
    container_name: micro-mockba-asset-futures-bot
    restart: always
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
      - ./apolo.log:/app/logs/apolo.log

  # ── Dashboard API (FastAPI) — uses root Dockerfile ─────
  dashboard:
    build: .
    container_name: mockba-dashboard-api
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data:ro
      - ./apolo.log:/app/apolo.log:ro
    environment:
      - DB_PATH=/app/data/trading.db
      - LOG_PATH=/app/apolo.log
      - MODEL_PATH=/app/data/signal_model.json
    command: python -m uvicorn dashboard.main:app --host 0.0.0.0 --port 8080 --log-level info

  # ── Dashboard UI (React + nginx) ─────────────────────────
  dashboard-ui:
    build: ./dashboard-ui
    container_name: mockba-dashboard-ui
    restart: always
    ports:
      - "85:80"
    depends_on:
      - dashboard

  watchtower:
    image: containrrr/watchtower
    container_name: watchtower-apolo-asset
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_POLL_INTERVAL=60
      - WATCHTOWER_LIFECYCLE_HOOKS=true
      - WATCHTOWER_LABEL_ENABLE=true